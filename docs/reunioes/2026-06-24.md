# 24/06/2026
- [x] Robo cobid - crons
   Entrar no server cotplan primeiro, depois.
   Server DGTI
   ssh -l marcelo 152.92.250.144

   Projeto = /home/srv-dinfo/iprj_covid_news/. Crontab tem 2 jobs (diário seg-sáb 00:00, semanal dom 00:00), ambos chamam docker-compose run web.

   ⚠️ crontab FILE substitui o crontab inteiro do usuário. Antes de instalar: ver o que já existe + decidir qual usuário.

   1. Cron chama docker-compose → dono do crontab precisa de acesso Docker (grupo docker) ou ser root. Confere quem tem:

   groups marcelo; echo "---"; groups srv-dinfo 2>/dev/null

   2. Ver crontabs existentes (não sobrescrever sem saber):

   echo "== marcelo =="; crontab -l 2>/dev/null
   echo "== srv-dinfo =="; sudo crontab -l -u srv-dinfo 2>/dev/null
   echo "== root =="; sudo crontab -l 2>/dev/null

   Cola as 2 saídas. Aí digo o comando de instalar pro usuário certo (provável srv-dinfo, dono do projeto), com backup antes. Algo tipo:

   sudo crontab -l -u srv-dinfo > ~/crontab.bak.$(date +%F) 2>/dev/null   # backup
   sudo crontab -u srv-dinfo /home/srv-dinfo/iprj_covid_news/cron_scripts/crontab

   Mas confirma os groups/existentes primeiro.

   3. Nao colocar o diário, só o semanal

   ## Resultado
   Cron rodando, problema de atualização, scripts e bloqueios:
   Diagnóstico completo. 3 spiders, 3 causas diferentes de falha — nenhuma insere nada:

   ┌─────────────┬───────────────────────────────────┬───────────────────────────────────┐
   │   Spider    │               Erro                │               Causa               │
   ├─────────────┼───────────────────────────────────┼───────────────────────────────────┤
   │             │ SSL handshake falha (unexpected   │ Stack TLS antiga                  │
   │ ArcaFioCruz │ eof)                              │ (Twisted/pyOpenSSL) não fecha     │
   │             │                                   │ handshake com servidor            │
   ├─────────────┼───────────────────────────────────┼───────────────────────────────────┤
   │             │ 403 no search.bvsalud.org         │ Site bloqueando bot (falta        │
   │ WhoInt      │ (segunda request, a que baixa     │ User-Agent válido / anti-scraping │
   │             │ dados de fato)                    │  novo)                            │
   ├─────────────┼───────────────────────────────────┼───────────────────────────────────┤
   │ PubMed      │ 403 na primeira request já        │ Mesma causa: NCBI bloqueando      │
   │             │                                   │ scraper                           │
   └─────────────┴───────────────────────────────────┴───────────────────────────────────┘

   Padrão comum nos 2 últimos: 403 Forbidden — sites adicionaram proteção anti-bot (Cloudflare, WAF, ou checagem de User-Agent/headers) desde que spiders foram escritos. Não é falta de notícia — é bloqueio ativo.

   Causa raiz real: projeto abandonado tecnicamente — libs desatualizadas (SSL) + sites-alvo endureceram defesas (403). Consertar exigiria:
   1. Atualizar User-Agent/headers nos spiders WhoInt/PubMed pra simular browser real.
   2. Atualizar Twisted/pyOpenSSL no requirements.txt do container pra resolver TLS do FioCruz.
   3. Possível necessidade de retry com proxy/headless browser se 403 persistir (WAF avançado).

   Isso é trabalho de dev (não infra). Quer que eu já mexa nos spiders (User-Agent) como teste rápido, ou só documentar achado pro time decidir prioridade?


-[ ] Criar um json com os dados das pessoas com base no grapho, serve como exemplo futuro da base de dados da uerj.
   - users
   - localBusiness
   - job (cargo) - plano de cargos (achar o plano de cargos na uerj)
   - duty (é a descrição(descrição do cargo) unica do cargo) - um pra um com jog
   - userjobs 


   com base nos documentos de entrada (currilos etc) e criação dos grafos e principalmente com dados do manual de cargos da uerj (vc encontra em c:\temp)  precisamo fazer um levantamento dos campos utilizados como (nome, cursos, localBusiness(local de trabalho), job(cargo), descrição do cargo (duty) um pra um com jog, userjobs etc. e montarmos uma estrutura .json que represente um modelo de banco de dados que servirá no futuro para que dados possam ser adaptados/migrados pra esse modelo que estamos criando. Depois crie um banco modelo encaixando as pessoas já existentes nos dados de entrada na nova estrutura de dados criada. Através desse modelo poderemos gerar os graphos, sem depender da extração das informações dos documentos. TEnte achar informações de lotação e localização em https://www.uerj.br/a-uerj/institucional/pessoas-e-unidades/ 