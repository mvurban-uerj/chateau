# Ver com o guaraldi/cotplan pq a máquina do LTI está desligado/sem acesso.

# Fazer a interface gráfica chamar o script do modelo pra executar o comando de pesquisa.
cd ~/chateau/model/chateau-cypher-orquestrador-v1.0.1/cliente  
./exemplo_http.sh ../exemplo/schema_compacto.txt "Quantas livros existem?"

./exemplo_http.sh ../exemplo/schema_compacto.txt  "Quantas artigos o Edson produziu em 2025?"

Se perguntar quantas bananas tem numa penca vai retornar null ou zero. Então o sistema precisa tratar isso.

- Ao fazer a busca o modelo retorna um query cypher, daí vai precisar fazer a busca no banco e retornar o conteúdo pro usuário. Além disso colocar tb a query gerada pelo modelo junto da resposta pro edson ver a saída.

# prointec@chateau:~/chateau/model/chateau-cypher-orquestrador-v1.0.1$
copiar o tar.gz  tem as instruções que o Edson quer pra interface quer fazer com o modelo.

# criar uma estratégia pra popular os dados no banco.
Ver esse link: https://github.com/users/edmlucas/projects/3?pane=issue&itemId=249480933&issue=edmlucas%7Cchateau-data%7C16