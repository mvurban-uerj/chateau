# Em Chateau-data
 
- [ ] criar um arquivo de dicionario que vai ajudar a mapear os campos de-para no formato padrão clean.
O script pega o modelo padrão, mas não há correspondencias certas, precisamos gerar um complemento que minimize essa dificuldade.

# Covid
- [ ] criar uma chave https no nginx - certificado gratuito e renovável.

# Sucupira
Existe api que extrai dados do sucupira (verde). Ver se já existe algum programa que faz a extração dos dados.
Usar a api pra ajudar na criação do dicionário